# newengine-ecs-runtime

Engine-owned `engine.ecs` gateway runtime service.

This crate exposes world summary/snapshot/command service handling and registers the gateway route candidate. Product profiles decide whether to install it.
