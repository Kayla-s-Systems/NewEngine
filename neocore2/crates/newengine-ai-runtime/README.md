# newengine-ai-runtime

Single-purpose replaceable baseline gateway provider.

This crate owns exactly one engine gateway implementation and deliberately does not register sibling gameplay capabilities. Product composition belongs above this crate.
