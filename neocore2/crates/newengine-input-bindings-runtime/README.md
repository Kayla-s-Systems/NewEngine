# newengine-input-bindings-runtime

Engine-owned `engine.input.bindings` service runtime. It stores/canonicalizes the active input profile, persists it under config, resolves semantic action frames from raw input and registers the engine-owned gateway route.
