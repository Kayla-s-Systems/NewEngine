# newengine-material-domain-standard

Generic standard lit material-domain provider used by editor and non-game-specific runtime profiles.

The current shader manifest file keeps its historical `gameready_lit.pipeline.json` filename for asset compatibility; provider identity, pipeline keys and code ownership are generic.
