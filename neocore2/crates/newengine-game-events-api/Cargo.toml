[package]
name = "newengine-game-events-api"
version = "0.1.0"
edition = "2021"
readme = "README.md"
description = "Stable game message/event contracts for NewEngine"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
