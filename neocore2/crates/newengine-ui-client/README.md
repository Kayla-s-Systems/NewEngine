# newengine-ui-client

Thin provider-neutral client for the `engine.ui` gateway. Domain runtimes publish state and request
draw packets through this crate instead of depending on the engine composition root.
