[package]
name = "newengine-ulog-api"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "North Star ULOG facade macros for engine-side structured logging migration"

[dependencies]
log = "0.4"
serde_json = "1"

[features]
default = []
serde = []
