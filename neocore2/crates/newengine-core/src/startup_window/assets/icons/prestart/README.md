# PreStart SVG Icon Pack

These are the real source icons for the NewEngine PreStart window.

Location:

```text
startup_window/assets/icons/prestart/*.svg
```

The Rust painter icons are only fallback/runtime drawing helpers. These SVG files are the authoritative editable UI assets.

Icon names:

```text
logo
project_cube
terminal
renderer_chip
renderer
monitor
screen_mode
check
fullscreen
bookmark
puzzle
clock
save
launch
cancel
folder
settings
core
physics
audio
input
ui
animation
script
status_enabled
status_disabled
```
