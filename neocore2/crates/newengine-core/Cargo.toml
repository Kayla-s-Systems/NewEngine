[package]
name = "newengine-core"
version = "0.1.0"
readme = "README.md"
edition = "2021"

[features]
# The default build provides a "batteries included" runtime facade (assets, console service, render API).
# The kernel/orchestrator-only build is obtained by disabling default features.
default = ["runtime"]

# Runtime facade: asset manager wiring, importer auto-registration, console service.
runtime = ["dep:newengine-ui"]

[dependencies]
crossbeam-channel = "0.5"
log = "0.4.29"
raw-window-handle = "0.6.2"
abi_stable = "0.11"
newengine-plugin-api = { path = "../newengine-plugin-api" }
newengine-plugin-host = { path = "../newengine-plugin-host" }
newengine-platform-api = { path = "../newengine-platform-api" }
newengine-math = { path = "../newengine-math" }
newengine-service-api = { path = "../newengine-service-api" }
newengine-ui-draw = { path = "../newengine-ui-draw" }

# Optional runtime dependencies. Kernel/orchestrator builds should disable default features.
newengine-ui = { path = "../newengine-ui", optional = true }

serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
parking_lot = "0.12.5"
sysinfo = { version = "0.38.3", default-features = true }

# Windows-only GPU/DX probe
[target.'cfg(windows)'.dependencies]
windows = { version = "0.62.2", features = [
    "Win32_Foundation",
    "Win32_Graphics_Dxgi",
    "Win32_Graphics_Dxgi_Common",
    "Win32_Graphics_Direct3D",
    "Win32_Graphics_Direct3D12",
] }
