# newengine-scene-bridge-runtime

Owns scene bootstrap, ECS scene projection, engine-view gateway state and editor scene adaptation.
The composition root only assembles this runtime; render-world consumes its explicit public bridge.
