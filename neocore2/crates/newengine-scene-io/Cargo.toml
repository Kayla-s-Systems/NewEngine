[package]
name = "newengine-scene-io"
version = "0.1.0"
edition = "2021"
description = "Scene IO service contract and host-side client for NewEngine"
license = "Proprietary"

[dependencies]
abi_stable = { version = "0.11" }
serde_json = "1"
newengine-plugin-api = { path = "../newengine-plugin-api" }
