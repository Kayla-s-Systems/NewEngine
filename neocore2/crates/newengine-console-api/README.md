# newengine-console-api

Provider-neutral DTOs and stable identifiers for the optional `engine.command`
gateway. This crate contains no registry, parser, service implementation or host
bootstrap behavior.
