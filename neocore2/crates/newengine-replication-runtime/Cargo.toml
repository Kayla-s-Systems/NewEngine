[package]
name = "newengine-replication-runtime"
version = "0.1.0"
edition = "2021"
readme = "README.md"
description = "Replication descriptor registry for NewEngine"

[dependencies]
newengine-replication-api = { path = "../newengine-replication-api" }
parking_lot = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
