[package]
name = "newengine-input-actions-api"
version = "0.1.0"
edition = "2021"
readme = "README.md"
description = "Semantic input action DTOs and engine.input.actions service contract for NewEngine"

[dependencies]
serde = { version = "1", features = ["derive"] }
newengine-camera-api = { path = "../newengine-camera-api" }
newengine-service-api = { path = "../newengine-service-api" }
