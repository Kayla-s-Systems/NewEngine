# newengine-input-actions-api

Semantic input action contract. This crate owns action definitions, action frame DTOs, dispatch events and the `engine.input.actions` service constants. It does not own any product/game default binding profile.
