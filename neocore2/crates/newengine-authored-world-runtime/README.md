# newengine-authored-world-runtime

Generic authored-world bootstrap for `.ymap` v2. The runtime consumes `engine.assets.maps` and `engine.assets.definitions` DTOs and materializes scene/model actors. It owns no gameplay, mission, FPS, terrain generator or day/night policy.
