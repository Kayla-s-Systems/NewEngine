# newengine-gameplay-fps

Profile-owned first-person gameplay package. It contains FPS-only execution systems and registers them through the generic NewEngine gameplay provider boundary.

The reusable engine runtime must not depend on this crate. Runtime profiles such as GameReady compose it explicitly.
