# newengine-input-capture-api

Neutral policy message describing which runtime input channels a UI/gameplay provider captures. It has no gameplay, UI implementation, input backend, renderer, ECS, or Host dependencies.
