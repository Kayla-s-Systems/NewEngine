# newengine-render-world-runtime

Cohesive render-domain runtime: world extraction, render policy, GPU caches/resources, shadows,
post-FX, visibility and frame submission. It consumes explicit camera/scene/gameplay contracts and
does not depend on `newengine-engine-runtime`.
