# newengine-viewport-bridge

Focused bidirectional UI/render viewport bridge. Owns only viewport extent, texture handle, camera input/frame, picking/framing requests, and external-preview redraw coordination. It has no renderer, editor, scene, asset, or product knowledge.
