# newengine-runtime-env

Instance-local compatibility configuration backed by the active `HostContext`. Runtime domains use
this instead of consulting process-global environment state after construction.
