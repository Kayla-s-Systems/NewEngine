# newengine-input-systems-runtime

Focused semantic input policy/runtime state. It observes raw/UI input, tracks semantic input subsystem health, applies capture/suppression policy, and exposes snapshots. It has no gameplay implementation, renderer, scene, ECS, backend, Host, or product knowledge.
