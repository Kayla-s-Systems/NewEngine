# newengine-runtime-policy

Immutable, typed runtime policy snapshots resolved from the active HostContext. Frame-critical
domain runtimes consume these values without consulting process-global environment state.
