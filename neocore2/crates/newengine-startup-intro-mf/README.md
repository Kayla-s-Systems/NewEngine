# newengine-startup-intro-mf

Windows presenter provider for `newengine-startup-intro`.

The provider uses native Win32 for the transient boot surface and Windows Media Foundation/MFPlay for decode and EVR-backed video presentation. It contains no descriptor or sequencing policy. Install it from a Windows host composition with `newengine_startup_intro_mf::install()`.
