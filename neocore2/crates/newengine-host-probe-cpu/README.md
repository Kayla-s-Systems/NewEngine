# newengine-host-probe-cpu

CPU topology and ISA feature probe only.

These leaf crates deliberately expose data/functions, not Host service registration. Composition belongs to `newengine-host-capabilities-runtime`.
