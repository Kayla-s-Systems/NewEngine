# newengine-asset-preview-runtime

Focused visual asset preview runtime. It resolves assets only through stable assets/model/material gateways, publishes a render draw-list provider, and coordinates an external preview viewport. It has no dependency on `newengine-engine-runtime`, concrete model/material runtimes, editor implementation, physics, gameplay, or a render backend.
