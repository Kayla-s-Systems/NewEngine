[package]
name = "newengine-gizmo"
version = "0.1.0"
readme = "README.md"
edition = "2021"

[dependencies]
newengine-math = { path = "../newengine-math" }
egui = { version = "0.33", optional = true }

[features]
default = []
egui = ["dep:egui"]
