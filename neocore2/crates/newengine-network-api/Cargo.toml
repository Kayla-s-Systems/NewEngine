[package]
name = "newengine-network-api"
version = "0.1.0"
edition = "2021"
description = "Provider-neutral networking contracts for NewEngine"

[dependencies]
newengine-replication-api = { path = "../newengine-replication-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
