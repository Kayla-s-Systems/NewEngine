[package]
name = "newengine-camera-api"
version = "0.1.0"
readme = "README.md"
edition = "2021"

description = "Stable camera gateway/protocol contract for NewEngine camera runtime providers"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
newengine-service-api = { path = "../newengine-service-api" }
