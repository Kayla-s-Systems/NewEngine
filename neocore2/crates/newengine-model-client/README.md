# newengine-model-client

Provider-neutral typed client for the stable `engine.assets.models` gateway. It contains no model implementation, import, scene, renderer, asset decoding, or provider registration logic.
