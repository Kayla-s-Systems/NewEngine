# newengine-world-runtime

Runtime-hosted provider for `engine.world`.

This crate does not replace `engine.scene` and does not expose ECS internals. It coordinates the living runtime world instance while `engine.scene` remains the authored structure contract.
