[package]
name = "newengine-ui-navigation-api"
version = "0.1.0"
edition = "2021"
readme = "README.md"
description = "Declarative menu navigation contracts for NewEngine UI surfaces"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
