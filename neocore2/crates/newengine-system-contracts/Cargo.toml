[package]
name = "newengine-system-contracts"
version = "0.1.0"
readme = "README.md"
edition = "2021"
description = "NewEngine system-layer value contracts: overlay status, task status, settings impact, diagnostics and recovery."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
