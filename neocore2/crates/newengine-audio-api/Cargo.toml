[package]
name = "newengine-audio-api"
version = "0.1.0"
edition = "2021"
readme = "README.md"
description = "Stable engine-facing audio service contract for NewEngine"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
newengine-service-api = { path = "../newengine-service-api" }
