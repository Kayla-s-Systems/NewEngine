[package]
name = "newengine-host-probe-input"
version = "0.1.0"
edition = "2021"
description = "Single-purpose host input-device presence probe for NewEngine"

[dependencies]
newengine-host-capabilities-api = { path = "../newengine-host-capabilities-api" }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62.2", features = ["Win32_UI_WindowsAndMessaging"] }
