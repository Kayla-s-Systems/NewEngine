# newengine-host-capabilities-api

Pure DTO and gateway contracts for Void Engine Host `PreInit`.

Public gateway: `engine.host.capabilities`.
The contract contains no OS probing, window toolkit, renderer, or runtime implementation.
