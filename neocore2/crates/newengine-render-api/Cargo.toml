[package]
name = "newengine-render-api"
version = "0.1.0"
readme = "README.md"
edition = "2021"

description = "Stable render value/protocol contract for NewEngine render plugins"
license = "MIT OR Apache-2.0"

[dependencies]
newengine-ui-draw = { path = "../newengine-ui-draw" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
