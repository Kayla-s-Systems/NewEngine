# newengine-camera-gateway-runtime

Authoritative `engine.camera` gateway and gameplay-camera projection bridge. Camera semantics live
here; the engine composition root only owns lifecycle/composition of this capability.
