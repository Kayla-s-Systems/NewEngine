[package]
name = "newengine-time-api"
version = "0.1.0"
edition = "2021"
readme = "README.md"
description = "Engine time gateway API and DTOs for NewEngine"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
newengine-service-api = { path = "../newengine-service-api" }
