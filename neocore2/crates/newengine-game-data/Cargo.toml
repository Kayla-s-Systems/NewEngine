[package]
name = "newengine-game-data"
version = "0.2.0"
edition = "2021"
description = "Project-authored runtime/game configuration schema and provider boundary"
license = "Proprietary"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
