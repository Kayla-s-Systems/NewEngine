[package]
name = "newengine-service-api"
version = "0.1.0"
readme = "README.md"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
newengine-contract-api = { path = "../newengine-contract-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
